

Get-ChildItem -Path . -Recurse -File |
  Where-Object { $_.Extension -eq '.php' -or $_.Extension -eq '.html' } |
  Sort-Object FullName |
  ForEach-Object {
    Write-Output "===== $($_.FullName) ====="
    Get-Content $_.FullName
    Write-Output "-------------"
  } | Out-File .\all_frontend_code.txt -Encoding utf8
