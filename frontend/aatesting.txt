Get-ChildItem -Path . -Recurse -File |
  Where-Object { $_.Extension -eq '.php' -or $_.Extension -eq '.html' -or $_.Extension -eq '.sql' } |
  Sort-Object FullName |
  ForEach-Object {
    Write-Output "===== $($_.FullName) ====="
    Get-Content $_.FullName
    Write-Output "-------------"
  } | Out-File .\aall_frontend_code.txt -Encoding utf8

